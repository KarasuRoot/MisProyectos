English:

To solve the "crackme_2" crackme—which asks for a name and then a serial number:

It was verified that this crackme takes the name and converts it using decimal values according to the ASCII table -> it sums them together, and the result is the serial number we are looking for.

For example, if the username is "pepe" -> the serial number is 426 = Why?

    'p' -> 112 (ASCII) -> '70' (hexadecimal) -> 112 (decimal)

    'e' -> 101 (ASCII) -> '65' (hexadecimal) -> 101 (decimal)

    'p' -> 112 (ASCII) -> '70' (hexadecimal) -> 112 (decimal)

    'e' -> 101 (ASCII) -> '65' (hexadecimal) -> 101 (decimal)

112 + 101 + 112 + 101 = 426

-----------------------------------------
-----------------------------------------
-----------------------------------------

Español: 
Para resolver el crackme "crackme_2" el cual pide un nombre y luego un serial -->
Se verifico que ese crackme toma el nombre, lo convierte con valores decimales segun la tabla ascii -> 
los suma entre si y el resultado es el serial buscado 
Por ejemplo si el usaurio es pepe -> el serial es 426 = ¿por que?
'p' -> 112 (ASCII) -> '70' (hexadecimal) -> 112 (decimal)
'e' -> 101 (ASCII) -> '65' (hexadecimal) -> 101 (decimal)
'p' -> 112 (ASCII) -> '70' (hexadecimal) -> 112 (decimal)
'e' -> 101 (ASCII) -> '65' (hexadecimal) -> 101 (decimal)
112 + 101 + 112 + 101 = 426